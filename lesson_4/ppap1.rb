flintstones = ["Fred", "Barney", "Wilma", "Betty", "Pebbles", "BamBam"]

# hash = {}
# flintstones.each_with_index do |name, index|
#   hash.store(name, index)
# end
# hash

# flintstones.each_with_index.to_h

# flintstones.each_with_object({}) do |name, hash|
#    hash.store(name, flintstones.index(name))
# end

# flintstones.map { |name| [name, flintstones.index(name)] }.to_h
